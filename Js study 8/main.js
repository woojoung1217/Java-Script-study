//Getter , Setter 값

// 요소안에 text 값을 textcontent 로 가져와 출력 
const boxEl = document.querySelector('.box');

console.log(boxEl.textContent);

//요소안에 text 값을 설정 

boxEl.textContent='woojoung';

console.log(boxEl.textContent);